"""
Django Admin Configuration for Connection Module.

This module is reserved for registering connection-related models with the
Django admin interface. Connections represent the relationships and communication
channels between organisations in the data space.

Registered Models:
    - None currently active (Connection model registration is commented out)

Note:
    The Connection model registration is currently disabled. Enable it by
    uncommenting the registration line if admin management of connections
    is required.
"""

# Connection model registration is currently disabled
# Uncomment the following line to enable admin interface for Connection model:
# myadminsite.register(Connection)
