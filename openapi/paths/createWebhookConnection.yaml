summary: Create webhook connection
description: This endpoint is used to create webhook connection
operationId: "createWebhookConnection"
requestBody:
  content:
    application/json:
      schema:
        $ref: "../definitions/ConnectionWebhookEvent.yaml"
  required: true
responses:
  "200":
    description: "OK"
tags:
  - webhook
